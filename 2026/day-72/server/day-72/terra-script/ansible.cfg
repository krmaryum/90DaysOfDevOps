[defaults]
inventory = inventory.ini
# private_key_file = ./terra-automate-key    # I commented it bcz it is already in inventory file
host_key_checking = False
timeout = 30
forks = 10

vault_password_file = .vault_pass
roles_path = roles

[ssh_connection]
pipelining = True
scp_if_ssh=True

