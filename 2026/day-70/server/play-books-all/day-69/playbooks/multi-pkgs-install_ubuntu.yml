---
- name: Multi pkgs install
  hosts: all_servers
  become: true

  tasks:
    - name: Install Multi packages
      apt:   # apt(name=[git, curl, wget], state=present)
        name:
          - git
          - curl
          - wget
        state: present
      when: ansible_facts['os_family'] == "Debian"
